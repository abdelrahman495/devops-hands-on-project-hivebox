kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: hivebox-cluster
nodes:
  - role: control-plane
  - role: worker
    extraPortMappings:
      - containerPort: 30080
        hostPort: 80
        protocol: TCP
