apiVersion: v1
kind: Service
metadata:
  labels:
    app: hivebox
  name: hivebox-service
spec:
  ports:
    - name: 8080-8000
      port: 8080
      protocol: TCP
      targetPort: 8000
  selector:
    app: hivebox
  type: ClusterIP
status:
  loadBalancer: {}
